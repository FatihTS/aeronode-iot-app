# Full URL (including /api) of the backend API.
# Leave unset for local dev — requests fall back to "/api" and go through
# the Vite proxy defined in vite.config.js.
VITE_API_BASE=https://your-backend.up.railway.app/api
